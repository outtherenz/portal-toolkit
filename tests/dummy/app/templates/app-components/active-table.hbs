<header class="article-header">
  <h1><span class="title-category">Component:</span> Active Table</h1>
</header>

<p>Like a normal table, but the columns are sortable.</p>

{{#active-table columns=tableColumns sortedBy=sort}}
  {{#each tableContent as |item|}}
    <tr>
      <td>{{item.letters}}</td>
      <td>{{item.dates}}</td>
      <td>{{item.numbers}}</td>
    </tr>
  {{/each}}
{{/active-table}}

<textarea>\{{#active-table columns=tableColumns sortedBy=sort}}
\{{#each tableContent as |item|}}
  tr
   td\{{item.letters}}/td
     td\{{item.dates}}/td
    td\{{item.numbers}}/td
  /tr
  \{{/each}}
\{{/active-table}}</textarea>
