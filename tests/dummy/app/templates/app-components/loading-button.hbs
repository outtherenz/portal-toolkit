<header class="article-header">
  <h1><span class="title-category">Component:</span> Loading Button</h1>
</header>

{{loading-button action='save' isLoading=isSaving isDisabled=false}}

<button {{action 'reset'}}>Reset</button>

{{buttonCode1}}

<h1>Disabled</h1>
{{loading-button action='save' isLoading=isSaving isDisabled=true}}

<button {{action 'reset'}}>Reset</button>

{{buttonCode2}}

<h1>Custom Text</h1>
{{#loading-button action='save' isLoading=isSaving isDisabled=false}} test {{/loading-button}}

<button {{action 'reset'}}>Reset</button>

{{buttonCode3}}


{{#page-description title='Loading Button'}}
<h1>Variables</h1>
  <h3>action</h3>
  <p>The action to be triggered upon clicking. Must be defined in the controller of the page</p>
  <h3>isLoading</h3>
  <p>this should be assigned to a boolean value that changes based on whether you want it to be 'loading' (true) or not 'loading' (false)</p>
  <h3>isDisabled</h3>
  <p>this should be assigned to a boolean value that changes based on whether you want it to be 'loading' (true) or not 'loading' (false)</p>
  <h3>Using as a Block</h3>
  <p>Using this helper as a block helper ie. {{example}}. allows you to define the text of the button, otherwise this defaults to "Submit"</p>
{{/page-description}}
