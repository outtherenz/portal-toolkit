<header class="article-header">
  <h1><span class="title-category">Component:</span> Date Input</h1>
</header>

<p>
  Usage: <code>\{{date-input date=date select=(action (mut date))}}</code> where <code>date</code> is of type <code>Date</code>.
</p>

{{date-input date=date select=(action (mut date))}}

<p>Content below the input will be covered when the picker is active.</p>
