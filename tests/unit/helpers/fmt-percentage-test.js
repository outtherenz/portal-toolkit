import { fmtPercentage } from 'dummy/helpers/fmt-percentage';
import { module, test } from 'qunit';

module('Unit | Helper | fmt percentage');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fmtPercentage([42]);
  assert.ok(result);
});
