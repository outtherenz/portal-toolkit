import { fmtNumber } from 'dummy/helpers/fmt-number';
import { module, test } from 'qunit';

module('Unit | Helper | fmt number');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fmtNumber([42]);
  assert.ok(result);
});
