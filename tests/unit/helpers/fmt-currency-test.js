import { fmtCurrency } from 'dummy/helpers/fmt-currency';
import { module, test } from 'qunit';

module('Unit | Helper | fmt currency');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = fmtCurrency([42]);
  assert.ok(result);
});
