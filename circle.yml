machine:
  timezone: Pacific/Auckland
  node:
    version: 8.2.0
dependencies:
  cache_directories:
    - bower_components
  override:
    - npm install
    - bower install
  post:
    - npm rebuild node-sass
