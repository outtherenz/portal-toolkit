machine:
  timezone: Pacific/Auckland
  node:
    version: 6.1.0
dependencies:
  cache_directories:
    - node_modules
    - bower_components
  pre:
    - npm i -g bower
  override:
    - npm i
    - bower i
  post:
    - npm rebuild node-sass
